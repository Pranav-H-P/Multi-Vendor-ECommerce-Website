export enum UserRole{
    CUSTOMER,
    VENDOR,
    ADMIN
}

export enum SearchSortOrder{
    ASC,
    DSC,
    NONE
}

export enum Rating{
    HORRIBLE,
    POOR, 
    SATISFACTORY,
    GOOD,
    EXCELLENT
}

export enum VendorApprovalStatus{
    APPROVED,
    PENDING,
    REJECTED
}